const {default: CustomButton} = require('./CustomButton');
const {default: ButtonDetail} = require('./ButtonDetail');
const {default: HeaderTextTab} = require('./HeaderTextTab');

export {CustomButton, ButtonDetail, HeaderTextTab};
